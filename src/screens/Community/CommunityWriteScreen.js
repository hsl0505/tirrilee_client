import React from 'react';

import CommunityWriteComponent from '../../components/Community/CommunityWriteComponent';

export default function CommunityWriteScreen(props) {
  const {navigation} = props;
  return <CommunityWriteComponent navigation={navigation} />;
}
